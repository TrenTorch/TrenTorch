import numpy as np


def pairwise_row_dots(a: np.ndarray, b: np.ndarray) -> np.ndarray:
    """
    Given 2D arrays a (shape (m, k)) and b (shape (n, k)), return
    an (m, n) array where result[i, j] is the dot product of
    a's row i and b's row j. Use matrix multiplication with a
    transpose (a @ b.T) — do not use an explicit loop.
    """
    pass


def matrix_vector_product(matrix: np.ndarray, vector: np.ndarray) -> np.ndarray:
    """
    Return the matrix-vector product of `matrix` (shape (m, n))
    and `vector` (shape (n,)), using @. The result should be a
    1D array of shape (m,).
    """
    pass
