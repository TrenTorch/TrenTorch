import numpy as np


def elementwise_multiply(a: np.ndarray, b: np.ndarray) -> np.ndarray:
    """
    Return the element-wise product of a and b (same shape),
    using the * operator.
    """
    pass


def increment_in_place(arr: np.ndarray, amount: float) -> None:
    """
    Mutate `arr` in place, adding `amount` to every element,
    using += (not arr = arr + amount). Return nothing.
    """
    pass


def square_each(arr: np.ndarray) -> np.ndarray:
    """
    Return a NEW array containing the square of every element in
    `arr`, without mutating `arr`.
    """
    pass
