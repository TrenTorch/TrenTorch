def assemble_stream(chunks: list[str]) -> list[str]:
    """Given the chunks of a streamed response, in arrival order, return the
    list of cumulative text snapshots -- the text as it would have looked on
    screen after each chunk arrived. `snapshots[i]` is the concatenation of
    `chunks[0..i]`.
    """
    # TODO: implement
    pass
