def build_audit_log(actions: list[tuple[str, str, str]]) -> list[str]:
    """Each action is (timestamp, actor, description). Format each one as
    "[timestamp] actor: description" and return the list of formatted lines,
    in the same order as given.
    """
    # TODO: implement
    pass
