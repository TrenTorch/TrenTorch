def latency_breakdown(events: list[tuple[str, float]]) -> dict[str, float]:
    """Each event is (component, duration). Return a dict mapping each
    component name to the sum of its durations across all events. Components
    not present in `events` don't appear in the result.
    """
    # TODO: implement
    pass
