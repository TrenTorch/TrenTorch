import math


def permutations_count(n, k):
    """
    n: total number of distinct items
    k: number of items to arrange, in order (0 <= k <= n)

    Returns:
        The number of ways to arrange k of n distinct items where ORDER
        matters: n! / (n-k)!, from Theory.
    """
    # TODO: Implement n! / (n-k)! from Theory.
    pass


def combinations_count(n, k):
    """
    n: total number of distinct items
    k: number of items to choose, order not mattering (0 <= k <= n)

    Returns:
        The number of ways to choose k of n distinct items where ORDER
        does NOT matter: n! / (k! * (n-k)!), from Theory.
    """
    # TODO: Implement n! / (k! * (n-k)!) from Theory.
    pass
