# Changelog

| Date | Description | Done by | Reviewed by | Merged by |
|---|---|---|---|---|
| 2026-08-21 | Imported TinyTorch from the cs249r_book dev branch as the fork base | Rocky | Rocky | Rocky |
| 2026-08-21 | Rewrote README with TrenTorch's own pitch, bolt mark, and Team Engineers section | Rocky | Rocky | Rocky |
| 2026-08-21 | Added contributor docs, adapted from cs249r-docs | Rocky | Rocky | Rocky |
| 2026-08-21 | Added GitHub Actions CI, adapted from TinyTorch's dev-branch validate workflow | Rocky | Rocky | Rocky |
| 2026-08-21 | Sped up CI: cached pip deps, shared Stage 1's build across Stages 2-5 | Rocky | Rocky | Rocky |
| 2026-08-21 | Fixed .gitignore swallowing bin/tito, which broke every CI stage | Rocky | Rocky | Rocky |
| 2026-08-21 | Fixed CI not tracking datasets/ and dropped an unsupported flag | Rocky | Rocky | Rocky |
| 2026-08-21 | Fixed Stage 6 auth against the private repo using GITHUB_TOKEN | Rocky | Rocky | Rocky |
| 2026-08-21 | Added multi-platform conversion pipeline (qmd, ipynb, txt, yaml) and CLI integration (PR #1) | Shivtej Gaikwad | Rocky | Rocky |
| 2026-08-21 | Aligned nbdev lib_path and removed an orphaned tito command | Shivtej Gaikwad | Rocky | Rocky |
| 2026-08-21 | Added dynamic path fix to pass CI Stage 6 on forks (PR #2) | Shivtej Gaikwad | Rocky | Rocky |
| 2026-08-21 | Renamed tito CLI to tren and tinytorch package to trentorch (PR #6) | Aadityansha | Rocky | Rocky |
| 2026-08-21 | Fixed concurrency race that cancelled main-branch CI on rapid merges, added status badge | Rocky | Rocky | Rocky |
| 2026-08-21 | Added multi-platform conversion pipeline and platform runtime adapter (own implementation) | Rocky | Rocky | Rocky |
| 2026-08-21 | Removed dangling tito entry point left after the tren rename merge | Rocky | Rocky | Rocky |
| 2026-08-21 | Restored correct TinyTorch/TrenTorch attribution broken by the blanket rename (PR #10) | Rocky | Rocky | Rocky |
| 2026-08-21 | Added bin/tren wrapper and fixed dangling tito imports left by the rename | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed test_06_autograd_progressive.py failing when run standalone | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed hardcoded tinytorch paths breaking every real export and check | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed CLI branding regressions blocking tests/cli/ collection | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed scripts/test-fresh-install.sh calling the deleted tito command | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed the real installed tren command being completely broken for every user | Rocky | Rocky | Rocky |
| 2026-08-22 | Renamed .tito/ to .tren/ for real, with a one-time migration, not a shim | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed Stage 7: preserved hand-written package files, fixed a stale test gate and a short milestone timeout | Rocky | Rocky | Rocky |
| 2026-08-22 | Removed tagline from README | Rocky | Rocky | Rocky |
| 2026-08-22 | Added Aadityansha to the Team Engineers section | Rocky | Rocky | Rocky |
| 2026-08-22 | Added contributor welcome bot and a live-updating CONTRIBUTORS.md | Rocky | Rocky | Rocky |
| 2026-08-22 | Added workflow_dispatch trigger to update-contributors.yml for manual runs | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed update-contributors.yml missing issues:read permission (PR #13) | github-actions bot | Rocky | github-actions bot |
| 2026-08-22 | Stopped the contributors bot from auto-merging its own PRs | Rocky | Rocky | Rocky |
| 2026-08-22 | Restored auto-merge for CONTRIBUTORS.md update PRs (PR #14) | github-actions bot | Rocky | github-actions bot |
| 2026-08-22 | Redesigned CONTRIBUTORS.md as an avatar grid, no emoji contribution-key | Rocky | Rocky | Rocky |
| 2026-08-22 | Used Rocky's custom avatar instead of the GitHub profile picture everywhere it appears | Rocky | Rocky | Rocky |
| 2026-08-22 | Added CHANGELOG.md covering full commit and merge history | Rocky | Rocky | Rocky |
| 2026-08-22 | SHA-pinned every third-party GitHub Action and added path filters to skip full CI runs on doc-only changes | Rocky | Rocky | Rocky |
| 2026-08-22 | Replaced duplicated per-stage checkout and setup steps across 6 jobs with one reusable workflow | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed a shell injection pattern in Stage 6 by moving a branch name out of an inline script and into an env var | Rocky | Rocky | Rocky |
| 2026-08-22 | Added Dependabot config for the github-actions ecosystem, then merged the 5 version-bump PRs it opened | Rocky | Rocky | Rocky |
| 2026-08-22 | Cached installed pip dependencies across CI jobs; first attempt broke jupyter by only caching site-packages, fixed by caching the scripts directory too | Rocky | Rocky | Rocky |
| 2026-08-22 | Scoped pytest-xdist parallelism to CLI and E2E tests only, after measuring no benefit for unit tests and a new failure under parallel integration tests | Rocky | Rocky | Rocky |
| 2026-08-22 | Restructured Stage 7 to stop rebuilding all 20 modules a second time and instead run milestones against Stage 1's already-built package | Rocky | Rocky | Rocky |
| 2026-08-22 | Fixed tren system reset silently never clearing progress, a leftover from the incomplete tito to tren rename | Rocky | Rocky | Rocky |
| 2026-08-22 | Decoupled Stages 5, 6, and 7 from Stages 2 to 4 so they run in parallel instead of waiting on the slowest of the three | Rocky | Rocky | Rocky |
| 2026-08-22 | Guarded 37 expensive analyze and benchmark demo blocks across 17 modules behind a CI check, kept full output for anyone running a module file directly | Rocky | Rocky | Rocky |
| 2026-08-22 | Ran module unit tests in process instead of spawning a subprocess per module, cutting Stage 1's real subprocess overhead | Rocky | Rocky | Rocky |
| 2026-08-27 | Fixed trentorch import failing for every `python -m platforms.cli.main` invocation, not just bin/tren, and several test-discovery paths silently reporting "0 tests, passed" after the data/ restructuring | Rocky | Rocky | Rocky |
| 2026-08-27 | Fixed CI build-artifact download landing in the wrong directory, a side effect of upload-artifact's least-common-ancestor path stripping | Rocky | Rocky | Rocky |
| 2026-08-27 | Found the real root cause of modules 08/09 being slow in CI: completing either one crossed a milestone's required-modules threshold and silently auto-ran a full real milestone during the maintainer's curriculum-verification loop; guarded it off for that loop only | Rocky | Rocky | Rocky |
| 2026-08-27 | Shrunk naive-Conv2d integration test inputs to full MNIST/CIFAR scale where only output shape was under test, cutting Stage 3 CI time 82-88% | Rocky | Rocky | Rocky |
| 2026-08-27 | Made command-module imports (numpy, trentorch, yaml) lazy: they were being paid on every single CLI invocation via main.py's eager instantiation of all 9 commands, regardless of which subcommand actually ran; cut `tren --help` from 2.7s to 0.8s | Rocky | Rocky | Rocky |
| 2026-08-27 | CI-scaled milestone 04's default training demo from 50 to 5 epochs; the milestone has no accuracy/epoch assertions of its own, pass/fail is just the return code, so this was the single largest line item in Stage 7 (164 of ~180 seconds) with zero real students affected (they still get the full 50-epoch experience) | Rocky | Rocky | Rocky |
| 2026-08-27 | Removed a duplicate nbdev re-export in Stage 1's inline test loop: `dev export` already exports each module to the package before `module complete` redundantly re-exported the same notebook to the same destination | Rocky | Rocky | Rocky |
| 2026-08-27 | Ran Stage 1's per-module export/complete calls in-process instead of spawning two subprocesses per module, cutting Stage 1 wall time roughly in half | Rocky | Rocky | Rocky |
| 2026-08-27 | Shrunk test/param scope across every remaining algorithmically-expensive test found by timing every module's unit tests: gradient-existence checks, dependency-integration checks, and a fusion-speedup benchmark with a 2000x2000 tensor and no assertion that actually needed it | Rocky | Rocky | Rocky |
| 2026-08-27 | End-to-end CI pipeline time: ~8m15s to ~3m47s (54% reduction), verified live on GitHub Actions after every fix | Rocky | Rocky | Rocky |
| 2026-09-01 | Fixed `tren module test` reporting a full pass on a completely unsolved notebook: Phase 1 runs data/src/ (which already contains the solution) and Phase 2 tests the installed trentorch package (which ships pre-built), neither reads the student's notebook. Added a Phase 0 that does (issue #71) | Maanas Tyagi | Rocky | Rocky |
| 2026-09-01 | Fixed `tren module complete` showing a truncated traceback with no actual error message on a failed unit test: `_parse_test_output()` kept the first 5 lines of the traceback instead of the last 5, so it always showed call-stack frames and cut off exactly before the real exception message. Verified with the real before/after traceback on an unsolved module 01 (issue #76) | Maanas Tyagi | Rocky | Maanas Tyagi |
| 2026-09-01 | Follow-up on the above: the `_parse_test_output()` fix alone didn't actually resolve #76, since `run_inline_unit_tests`'s verbose console-print loop -- the code path `tren module complete` actually prints through -- re-sliced the already-fixed error string with the identical first-N-instead-of-last-N bug a second time. Fixed both display sites (`run_inline_unit_tests`, `run_integration_tests`) plus a third, untouched instance of the same bug in `run_integration_tests`'s collection-error path. Verified live: the exact repro from #76 still showed zero exception-message lines with only the first fix applied; now shows the real `NotImplementedError` message end to end. Added a regression test asserting on the actual printed console output, not just `_parse_test_output`'s return value, since that gap is exactly how the incomplete first fix passed review | Rocky | Rocky | Rocky |
| 2026-09-01 | Fixed `tren --version` always printing "vunknown": `_get_version()` looked for pyproject.toml one folder too shallow (platforms/ instead of the repo root), so the file was never found and it silently fell back to "unknown" (issue #80) | Maanas Tyagi | Rocky | Rocky |
| 2026-09-01 | Fixed `tren olympics` telling students to run `tito community login`, a command that was fully removed, not renamed. Removed the dead instruction, and fixed two more lines on the same screen still telling students to run `tito module status` / `tito milestone status` instead of `tren` -- the tito to tren rename never reached this file (issue #81) | Maanas Tyagi | Rocky | Rocky |
| 2026-09-01 | Expanded `test_server.py` from 4 to 19 test cases covering every `tren serve` REST endpoint, SSE stream wire format, and security guards; also finished the `tito` to `tren` cleanup in `tren system health` and other CLI status output | Shivtej Gaikwad | Rocky | Rocky |
| 2026-09-01 | Fixed a typo in the new PR's own rebrand pass that dropped `  t` from one `tren dev export` hint (rendered as `ren dev export 01`), and fixed the real bug the new tests surfaced: `handler.py`'s subprocess calls used `text=True` with no explicit encoding, which falls back to `cp1252` on Windows and crashes on Rich's UTF-8 output -- hanging `tren serve`'s module-test SSE stream and module-complete response for any Windows user | Rocky | Rocky | Rocky |
| 2026-09-01 | Full-repo survey for leftover `tito`/`tinytorch` references after the rename. Found the celebration panel every student sees after `tren module complete` telling them to `from tinytorch import X`, a package that has been `trentorch` since PR #6 -- copy-pasting it crashes with ModuleNotFoundError. Fixed it, plus a dead `tito` quick-commands block in the dev setup script. Everything else found (jupyter kernel name, `.tinytorch` profile dir, `tren system update`'s stale directory layout, product-narrative branding) is either self-consistent internal naming or a separate, larger issue -- documented, not silently patched | Rocky | Rocky | Rocky |
| 2026-09-01 | Fixed a real CodeQL Security tab finding (py/http-response-splitting, alert #3): `do_OPTIONS` echoed the raw `Origin` header straight into the `Access-Control-Allow-Origin` response header. Confirmed exploitable live: an `Origin` value using RFC-2822-style folded-header syntax (e.g. `http://localhost/\r\n X-Injected: pwned`) still parses to a trusted hostname but smuggles an extra response header past the check -- reproduced a genuine spliced-in `X-Injected` header in the raw response before the fix, gone after it. Fixed by rejecting any Origin containing CR/LF before echoing it, regardless of what its hostname parses to | Rocky | Rocky | Rocky |
| 2026-09-01 | Follow-up on the CORS Origin fix above: the boolean CR/LF guard didn't actually satisfy CodeQL's own taint analysis -- re-scanning main after #87 merged still showed the alert open, since the query looks for a recognized sanitizer transformation (like `.replace()`) between the tainted source and the header sink, not a membership check. Rewrote it to build a `.replace()`-derived `safe_origin` and pass that to `send_header`, matching the exact pattern CodeQL's own alert recommends. Confirmed via re-scan: alert #3 now shows 0 results | Rocky | Rocky | Rocky |
| 2026-09-01 | Full docs/ audit (10 files, ~2000 lines): fixed three real leftover-TBD reviewer/merger cells in this file, then found and fixed extensive stale content in docs/ predating both the `data/` and `platforms/` restructurings -- wrong file paths throughout (`tren/commands/*` -> `platforms/cli/*`, `src/` -> `data/src/`, `trentorch/` -> `data/trentorch/`), a self-contradicting branding note in README.md (explained why docs say "TrenTorch" when it meant to explain "TinyTorch"), a completely wrong coding-style.md claiming no linter is enforced when ruff has been a required CI gate all along, wrong Jupyter kernel name and profile path (`tinytorch`/`~/.tinytorch/`, not `trentorch`/`~/.trentorch/`), wrong exception class name (`TinyTorchCLIError`, not `TrenTorchCLIError`), stale `.tren/*.json` paths (now `user_data/*.json`), an undercounted and internally-contradictory command registry count in deep-dive.md (said both "9" and "10", real answer is 12 entries/11 classes), two fully undocumented commands (`tren tui`/`dashboard`, `tren serve`) added to command-reference.md and implementation.md, a stale 3-phase description of `tren module test` missing the Phase 0 notebook-solved check added by issue #71, and several already-fixed "known issues" (INSTRUCTOR.md no longer exists, CONTRIBUTING.md's PyPI/tinytorch.org claim is gone) still listed as current. Left design.md's "Project history" section untouched since it's explicitly sourced from the upstream monorepo's own git log, not this fork's | Rocky | Rocky | Rocky |
| 2026-09-01 | Two branch/process governance changes: (1) branch protection now requires 1 approving review before merge, except repo admins (only Rocky currently) who already bypass via the existing `enforce_admins: false` setting -- no custom bot needed, just the native GitHub interaction between those two settings. (2) Added `require-linked-issue.yml`, a `pull_request_target`-based bot that flags (comments + `needs-linked-issue` label, non-blocking) any PR from outside the known collaborator list that doesn't reference an issue assigned to its own author -- enforces "open an issue, get assigned, then PR" for first-time/external contributors without touching the team's normal workflow. Documented both in CONTRIBUTING.md | Rocky | Rocky | Rocky |
| 2026-09-01 | Added a suite of maintenance bots: three work today (auto-label PRs by changed path via `.github/labeler.yml`, auto-label issues by keyword, open a tracking issue automatically for new CodeQL alerts live on main, and comment when settings.ini/pyproject.toml's versions drift apart). Three more (ruff autofix-and-push, auto-update PR branches behind main, CHANGELOG TBD-cell backfill on merge) are built and wired up but gracefully no-op until the AUTOFIX_PAT secret is added -- each logs a clear notice and skips rather than failing. Created 6 new labels (cli, dev-tools, curriculum, milestones, tests, security) the labelers need. Documented the autofix bot in CONTRIBUTING.md | Rocky | Rocky | Rocky |
| 2026-09-01 | Rewrote `validate_cli_docs.py` (the "auto-documentation update" ask): the old version hand-maintained a parallel VALID_COMMANDS list and scanned directories (site/modules/tests/milestones) that predate the data/ restructuring entirely, disabled since. Rewrote it to introspect the real, live CLI's argparse tree directly instead -- ground truth that can't drift out of sync with the code. First implementation had a real bug caught by mutation-testing it against the actual docs: trimming trailing words from the right to allow positional args (e.g. `tren module test NN`) also silently accepted any wrong subcommand (`tren module frobnicate` passed, since `tren module` alone was already valid). Fixed by walking the command tree level by level: a dispatch level's next word must be a real subcommand, a leaf level accepts anything after it as positional args. Verified clean against all 14 current markdown files, then confirmed the fix actually catches a deliberately wrong command. Re-enabled as a pre-commit hook and added as a new required CI check (`validate-docs.yml`) | Rocky | Rocky | Rocky |
| 2026-09-01 | Reset the project version from 0.1.13 to 0.0.1 across pyproject.toml, settings.ini, and the one docs mention, since this is meant to read as starting fresh. No downgrade guard exists in this fork's own release.sh (that guard referenced in design.md's Project history section is upstream's own release workflow, not this one) -- confirmed live: `tren --version` and the CLI's own dynamic-lookup test both already report v0.0.1 correctly, full local suite (471 tests) still green | Rocky | Rocky | Rocky |
| 2026-09-01 | Added two labels auto-applied the moment a PR/issue is opened, the closest real equivalent to a custom author badge, since GitHub's native comment badge (author_association) is a fixed OWNER/MEMBER/COLLABORATOR/CONTRIBUTOR/NONE set with no way to rename or extend it, confirmed not configurable through any repo or org setting: "Maintainer" for whoever holds Maintain/Admin repo access (checked live via the API, not a hardcoded list, so it stays correct as that roster changes), and "Core Engineer" for Maanas and Yashanand specifically (a named-person title, not a permission tier GitHub exposes, so that one's a plain username list) | Rocky | Rocky | Rocky |
| 2026-09-01 | Fixed `tren benchmark capstone` showing a fake "Complete! 90/100" score with 0 modules done: the "is Module 20 done" check tested whether trentorch/olympics.py could be imported, but that file ships pre-built and committed (same root cause as issue #71), so it always passed. Now checks the student's real completed_modules progress instead, and the still-placeholder "full" scoring path is now labeled as such rather than shown as a real result (issue #93) | Maanas Tyagi | Rocky | Rocky |
| 2026-09-01 | Follow-up on the above: `_run_simplified_capstone`, the exact fallback every under-Module-20 student now hits, had the identical bug one function over -- a hardcoded `basic_score: 75` (with a `time.sleep(1)` "simulating" real work) saved as a real result with no on-screen warning it was fake, only a buried "note" field in the JSON nobody reads. Fixed to match the pattern the original fix used for the full path: `"placeholder": True` in the saved result plus a clear on-screen warning. Added regression tests for all three cases (0-modules fallback doesn't show the old fake full score, the simplified fallback warns on-screen, and the full path still flags itself as a placeholder even once Module 20 is genuinely complete) -- confirmed all three fail against the pre-fix code with the exact right symptom before restoring the fix | Rocky | Rocky | Rocky |
| 2026-09-01 | Live end-to-end test of the AUTOFIX_PAT bots turned up two real bugs. (1) `pip install ruff>=0.6.0` was unquoted in both `autofix.yml` and `lint.yml`; bash parses the bare `>` as a redirect, so it silently ran `pip install ruff` (no version floor) into a stray file literally named `=0.6.0` -- harmless in `lint.yml` (ephemeral, no commit step) but `autofix.yml`'s real push actually committed that stray file to the branch. Confirmed live, then quoted both. (2) `codeql-alert-to-issue.yml`'s `code_scanning_alert` trigger had failed to schedule on *every single push* since PR #91 merged -- 13/13 failures, invisible until now since it's not a PR-check-list workflow `gh pr checks` shows. GitHub Free-tier orgs have known gaps supporting that event as an Actions trigger. Replaced it with a `schedule`-based poll against the code-scanning/alerts API instead, directly verifiable via workflow_dispatch rather than a trigger that fails silently. Deleted the throwaway test file this row's TBD is left for `changelog-backfill` to fill in | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | `update-contributors.yml` had been failing on every single scheduled run since it was added: its PR-open step used the default `GITHUB_TOKEN`, which this org doesn't permit to open PRs (`GitHub Actions is not permitted to create or approve pull requests`), confirmed via the actual failing run. The branch got pushed before that step failed, so it never got a PR and was left orphaned (`update-contributors-1788301995`), never auto-deleted. Switched it to `AUTOFIX_PAT`, the same token `autofix.yml` and `changelog-backfill.yml` already use successfully, with the same clean no-op if the secret is unset. Deleted the orphaned branch, then confirmed live via manual dispatch: it opened and auto-merged a real PR (#100) end to end | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | Full repo-wide sweep replacing every remaining `tinytorch`/`tito` reference with `trentorch`/`tren` (code, docs, config, datasets), no backward-compatible aliasing kept anywhere in this fork's own surface. Found and fixed real bugs along the way, not just text: `TinyTorchCLIError`/`TinyTorchStatusAnalyzer` renamed as live Python identifiers; the Jupyter kernel identifier renamed consistently at every registration/detection site; `TITO_ALLOW_SYSTEM` removed from `main.py`'s venv guard, with every still-setting caller (`server/handler.py`, `tui/app.py`, three test files, two CI workflow env blocks) updated to `TREN_ALLOW_SYSTEM` -- these were silent regressions the moment the alias dropped, caught only by grepping `tito` as a non-word-boundary substring, since `TITO_ALLOW_SYSTEM` has no `\b` before its underscore; `test_runner.py`'s `TINYTORCH PACKAGE NOT EXPORTED` string check had drifted from what `conftest.py` actually emits, silently breaking the progressive-export-gate carve-out behind a test whose fixture used the same stale string; `.editorconfig`/`.gitattributes`/`.gitignore` generated-file glob patterns still pointed at a top-level `tinytorch/` that hasn't existed since the `data/` restructuring, so their intended editor/linguist/ignore treatment was silently inactive at the real path. Deliberately left the CHANGELOG, the one-time maintenance scripts, `system/update.py` (points at the real upstream repo's own layout), and README/LICENSE's upstream-credit prose untouched, each for a documented reason. Full test suite (767 passed) shows zero new failures versus unmodified main | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | Created `dev` as the new default branch (same branch protection as `main`: 1 review, same 3 required checks); `main` stays the protected, stable branch, updated by merging `dev` into it periodically. Rewired every CI workflow and bot that hardcoded `branches: [main]` or `ref: main` to also watch/follow `dev` -- `changelog-backfill.yml` and `auto-update-behind-prs.yml` now follow the PR's real base branch instead of assuming main, `update-contributors.yml` and `codeql-alert-to-issue.yml` now target `dev`. Verified live: PR #127's full CI matrix and the 1-review block both worked correctly against `dev` | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | Moved the "what's new" announcement out of GitHub Discussion #98 into a real, versioned `docs/Changes.md`, linked from `docs/README.md`'s index -- discoverable and diffable instead of living only in a growing discussion thread. Discussion #98 itself updated with the newer bots (Dependabot auto-merge, OpenSSF Scorecard, Dependency Review, stale-assignment sweep, unified Contributors table) and the `dev`-as-default-branch change | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | Deleted the whole `platforms/dev_tools/tools/maintenance/` directory (`restructure-project.sh`, `merge-site-to-docs.sh`, `cleanup_history.sh`, `verify-restructure.sh`) plus `platforms/dev_tools/scripts/build-book.sh` and `build-docs.sh` -- all one-time or dead: the restructuring scripts describe transformations that already happened and were never meant to run again (`cleanup_history.sh` even hardcoded `/Users/VJ/GitHub/TinyTorch`, the original upstream maintainer's own machine path), and the Jupyter Book build scripts referenced a `docs/_build`/`website/` pipeline that predates the already-removed Quarto site, confirmed unreferenced by any CI workflow or Makefile. Updated the docs (`design.md`, `implementation.md`, `refactor-plan.md`) that pointed at these files to reflect the removal instead of leaving dangling references, and fixed one more doc bullet found along the way (`settings.ini`/`pyproject.toml` version-drift note, moot since `check-version-consistency.yml` already covers it) | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | Worked through all 17 open OpenSSF Scorecard findings on the Security tab. Fixed for real: 2 critical script-injection/untrusted-checkout patterns (`autofix.yml`'s branch name interpolated straight into a shell script instead of going through an env var; `check-version-consistency.yml` doing a real `actions/checkout` at a PR's head SHA under `pull_request_target`, replaced with fetching just the two files' text via the REST API so no PR code ever touches the runner's filesystem), 3 workflows with overly-broad or missing token permissions (moved workflow-level `permissions:` down to job-level in `codeql.yml`/`semantic-pr-title.yml`, added an explicit least-privilege block to `run-test-stage.yml` which had none), and pinned `ruff`/`rich`/`PyYAML` installs to exact versions instead of unbounded floors in `lint.yml`/`autofix.yml`/`validate-docs.yml`. Left 3 things as documented, not silent, exceptions: `requirements.txt`-based installs (`setup.sh`, `run-test-stage.yml`) stay on this project's own deliberate floor-range policy rather than a separate hash-locked file that isn't otherwise part of this project's dependency strategy; and 4 repo-level process metrics (Maintained, Code-Review, Fuzzing, CII-Best-Practices) aren't code bugs -- Code-Review in particular directly reflects this repo's own chosen admin-bypass-merge workflow, not an oversight | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | Credited McCulloch & Pitts' 1943 paper in Milestone 01's historical_context: Rosenblatt's 1958 Perceptron built directly on their 1943 artificial-neuron model, which wasn't mentioned anywhere in the milestone text | Maanas Tyagi | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-02 | Fixed `tren module resume/view/start`'s Jupyter server failure mode: when the URL couldn't be auto-detected, the error message pointed at "terminal output above" that was actually sent to DEVNULL and never printed anywhere. Now captured to user_data/jupyter.log, and the message points at that real file. Also fixed the deeper cause found while verifying this: `start_jupyter_server` launched a bare `jupyter lab` subprocess, which depends on PATH order to find the right installation -- on a machine where a different Python's `jupyter` resolves first, that other installation's subcommand discovery runs instead and silently fails to start anything. Switched to `sys.executable -m jupyterlab`, tied to the exact venv running `tren` itself. Widened the detection retry window from 10s to 20s for slower machines (issue #139) | Maanas Tyagi | Rocky | Rocky |
| 2026-09-03 | Fixup on PR #140 before merge: switching to `sys.executable -m jupyterlab` fixed the PATH bug but broke the existing "jupyterlab not installed" fast-fail path along the way -- `Popen()` can't raise `FileNotFoundError` for a missing module the way it could for a missing executable (`sys.executable` always exists), so a missing package now failed silently inside the child instead, and the function burned its whole (newly doubled) 20s window before reporting the misleading "started but couldn't detect it" message. Added an explicit post-launch poll check that restores the fast, accurate "Jupyter Lab not found" message. Also closed a real handle leak (`log_file` was never closed in the parent -- now a `with` block, closed right after the detached child gets its own inherited copy) and extended the same PATH-resolution fix to `find_running_jupyter_server`'s own `jupyter server list` call, which PR #140 had left on the same bare, PATH-dependent command. Added `test_jupyter_server_startup.py` (5 tests); verified with real mutation testing -- reverted the poll-check fix, confirmed the regression test fails exactly as expected, restored it | Rocky | Rocky | Rocky |
| 2026-09-03 | Full-repo bug hunt (100+ candidate patterns checked, 18 confirmed real). First fix: `get_venv_path()`'s `.tinyrc` read used `json.load(open(CONFIG_FILE))` with no `with` block and no explicit encoding, leaking an open file handle on every call (reproduced: ResourceWarning), and with no `encoding="utf-8"` fell back to cp1252 on Windows, so a `.tinyrc` containing a byte cp1252 has no mapping for raised `UnicodeDecodeError` that the surrounding `except Exception: pass` silently swallowed, dropping the user's custom `venv_path` with zero indication and falling back to the default `.venv`. Fixed with an explicit `with open(..., encoding="utf-8")`; added `test_virtual_env_manager_config.py`, verified with mutation testing: both new tests fail against the pre-fix code with the exact symptoms above, pass after | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Full-repo bug hunt (100+ candidate patterns checked, 18 confirmed real). Second fix: `TrenTorchStatusAnalyzer.analyze_module()`'s subprocess import-check embedded a module's Windows path straight into an f-string-generated Python script (`sys.path.insert(0, '{module_path}')`), with no escaping. A path segment like `\02_activations` is read back by the generated script's own parser as the octal escape `\x02`, corrupting the inserted path into one that doesn't exist: every module's import check was silently unreliable on Windows. Fixed by embedding both paths via `{str(path)!r}` instead of a raw f-string substitution, so they round-trip as proper Python string literals. Added `test_status_analyzer_windows_path_escape.py`; verified with mutation testing: fails against the pre-fix code (module import falsely reported as broken), passes after | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Full-repo bug hunt (100+ candidate patterns checked, 18 confirmed real). Third fix: `create_virtual_environment`'s Apple Silicon/Rosetta re-targeting built `f"arch -arm64 {python_exe} -m venv {venv_path}"` and ran it via `subprocess.run(..., shell=True)`. A `venv_path` containing a space (a real path, e.g. under "OneDrive - Company") silently split into multiple shell tokens instead of staying one argument, breaking venv creation. Rewrote to build a real argv list (`["arch", "-arm64", sys.executable, "-m", "venv", str(venv_path)]`) and drop `shell=True` entirely, no quoting needed for a shell that never runs. Added `test_rosetta_venv_creation_uses_argv_list_not_shell_string`; verified with mutation testing: fails against the pre-fix code (`shell=True` detected), passes after | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Full-repo bug hunt, fourth fix: `analyze_dataloader_performance()`'s `shuffle_overhead` calculation divided by `time_no_shuffle` with no floor, the same zero-elapsed-time-on-a-fast-Windows-timer class fixed repeatedly elsewhere this session. Not currently reachable from CI (the call site is commented out, an opt-in demo function), but a real crash the moment anyone uncomments it and runs on a fast enough machine. Floored the denominator; added `test_dataloader_shuffle_overhead_zero_latency.py`, verified with mutation testing: fails with `ZeroDivisionError` against the pre-fix code, passes after | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Full-repo bug hunt, fifth fix: Milestone 02's XOR-crisis demo computed the best random-search weight config each time a new best was found (`(w1, w2, b, "Random")`) but never assigned or used it, a bare tuple expression that does nothing (confirmed via `ruff` B018). The winning configuration was silently discarded the moment it was found. Now stored and printed alongside the existing "Best from random search" accuracy line. Added `test_xor_crisis_random_best_config.py`; verified with mutation testing: fails against the pre-fix code (no weights ever reported), passes after | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Full-repo bug hunt, sixth fix (the biggest of the set): every JSON state-file save in the CLI (`progress.json`, `milestones.json`, benchmark results, `profile.json`, submission/config files, 10 call sites across 5 files) used `open(path, "w")` followed by `json.dump(...)`. `open(path, "w")` truncates the file to 0 bytes the instant it's called, before a single byte of new content exists. Any interruption between that truncation and the write finishing (crash, Ctrl+C, power loss, OOM kill) leaves a truncated, unparseable file. Every read site for these files already wraps `json.load()` in a broad except that resets to an empty default on `JSONDecodeError`, so an interrupted save doesn't just lose the write in progress, it silently erases everything already saved. Reproduced end to end: simulated a truncated `progress.json`, confirmed the read path silently discarded 5 previously-completed modules with zero warning. Added `atomic_io.atomic_write_json()`, a shared write-to-temp-then-`os.replace()` helper (atomic on both POSIX and Windows), and converted all 10 sites (`setup.py`, `benchmark.py` x4, `milestone/system.py` x2, `module_workflow/reset.py`, `module_workflow/workflow.py`) to use it. Added `test_atomic_io.py`; verified with mutation testing against a naive `open(path, "w")` implementation: the interrupted-write regression test fails against it with the exact original-file-gets-clobbered symptom, passes against the real fix | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Full-repo bug hunt: removed two stray, inert leftover statements found via `ruff` B018 ("useless expression"): a bare `4` in `test_attention_core.py` and a bare `True` in `run_sandbox_tests.py`, both dead editing artifacts with zero behavioral effect. Confirmed inert (not silently masking something) via `ruff --select B018` (clean after) and the full local test suite for the affected file (identical pass count before/after) | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Fixed `tren dev test --inline` failing with "Jupytext not found" on a correctly-installed venv: `maintainer_use/.tinyrc` (checked into the repo) had `venv_path` set to `.` instead of `.venv`, so every tool lookup based on it looked in the project root instead of the actual venv and fell back to a nonexistent system-wide jupytext. Nothing in the codebase writes this file and `get_venv_path()` already defaults correctly to `.venv` without it, so deleted it rather than fixing the value. Verified: `tren dev test --inline --module 01` failed before, passed clean (1/1 modules) after (issue #156) | Maanas Tyagi | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Fixed `tren dev test --inline` hiding the real reason for an export failure unless `--ci` was passed: `_run_inline_tests()` captured the export step's real output into a quiet buffer, but only printed that buffer on failure inside an `if ci_mode:` block. A local, non-CI run that hit a real export failure got only "Failed at NN:export" with no way to self-diagnose (same failure class as issues #76 and #139). Now shown either way, styled for the console when not in CI mode (issue #158) | Maanas Tyagi | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Fixup on PR #161 before merge: the export-failure branch's fix left an identical sibling bug unfixed five lines below it, the module-complete-failure branch also only showed its buffered diagnostic output under `if ci_mode:`, so a local run whose module passed export but failed its own unit tests was still left with a bare "Failed" and nothing to self-diagnose. Fixed the same way as the export branch. Added `test_inline_tests_non_ci_diagnostics.py` (2 tests, covering both branches); verified with mutation testing: the module-complete test fails against the pre-fix code with the exact bare-"Failed" symptom, passes after | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Audit: every `progress.json`/`milestones.json` read site (8 across `workflow.py`, `milestone/system.py`, `milestone/command.py`, `benchmark.py`, `tui/app.py`, `server/handler.py`) caught a corrupted or unreadable file with a bare `except (OSError, json.JSONDecodeError): pass` and silently fell back to an empty default. A student whose real progress.json exists on disk but fails to parse just saw "0/20 modules complete" with zero indication their actual data might still be sitting right there, unreadable, indistinguishable from genuinely having no progress. Added `atomic_io.read_json_or_warn()`, a shared helper that keeps the same "don't crash the CLI" fallback behavior but prints exactly what happened and where (file path, real error, and what to do about it) before falling back, and converted all 8 sites to use it. Added `test_atomic_io_read_json_or_warn.py` (4 tests); verified with mutation testing against a naive silent-fallback implementation: the corruption tests fail against it with the exact zero-explanation symptom, pass after. Full affected suite (131 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Fixed `tren dev test --inline` silently marking real student progress complete: `complete_module()`'s maintainer-verification path (`TREN_DEV_VERIFY_SOLUTION=1`, reads the reference solution from `data/src/`, not the student's notebook) still called `update_progress()` unconditionally, writing to the same `user_data/progress.json` a real student uses. Since `tren dev` has no actual technical restriction ("maintainer-only" is a documentation label, not an enforced permission), any student could run `tren dev test --inline --all` themselves and instantly mark the entire course complete, unlocking every milestone, with zero code written. Gated `update_progress()` on the same env var already used elsewhere to distinguish this mode, so it now no-ops instead of writing. Verified live: ran the exact same command on module 04 before and after. Before, progress.json gained "04"; after, all 4 modules verified pass but progress.json stayed untouched (issue #168) | Maanas Tyagi | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Follow-up on the above: CI's own Stage 1 build failed on module 2 with "you must complete module 01 first". `complete_module()`'s sequential-order gate reads `completed_modules` from the same file `update_progress()` was just fixed to stop writing to in verify-solution mode, so module 01's completion was correctly never recorded there, and the very next module's own gate check then blocked on it. Skipped that gate the same way, in the same mode: `dev/test.py`'s `_run_inline_tests()` already enforces its own 01-to-20 order externally, so the internal gate serves no purpose there and only breaks progressive verification once the write it depended on stopped happening. Reproduced the exact CI failure locally first (reset progress.json to empty, ran modules 01 then 02), confirmed it failed the same way, then confirmed the fix clears it while progress.json stays untouched either way | Maanas Tyagi | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Review fixup on PR #169 before merge: neither the original loophole fix nor its CI-driven follow-up had an automated test, verified only by a manual before/after run described in the commit messages. For a progress-integrity fix, that leaves nothing to catch a future regression the way CI itself just caught one. Added `test_complete_module_verify_solution_mode_does_not_write_progress` (seeds `progress.json` with unrelated existing data and asserts it's byte-for-byte unchanged after a verify-solution run, not just missing the new module) and `test_complete_module_verify_solution_mode_does_not_block_on_sequential_gate` (runs modules 01 then 02 in verify-solution mode, the exact sequence that broke CI). Verified with mutation testing: reverting each fix in isolation fails the matching test with the exact original symptom (write test still passes when only the gate-skip is reverted, gate test fails with "module 02 was blocked" when only that revert is applied), both pass with the real fixes restored | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Removed `tren benchmark`'s dead-end community-submission flow: after every benchmark it asked "submit your results to the community?" (default yes) and, if you weren't "in the community," told you to run `tren community login` -- a command that was removed (same dead-command class as issue #81). The rest of the flow was equally phantom: a stub `_submit_to_website()` POSTing nowhere, a hardcoded `https://trentorch.ai` config with `enabled: False`, and a "Community leaderboard coming soon!" line, none of which exist in this fork. The real results already save locally under `user_data/benchmarks/` before that prompt ever ran, so the whole flow only prepared a redundant second file and walked users toward vaporware. Deleted `_prompt_submission`/`_get_community_data`/`_get_config`/`_submit_to_website` (a closed island only calling each other), both call sites, the now-pointless `--skip-submit` flag, and the now-unused `Confirm`/`json` imports (~145 lines); updated `command-reference.md` to match. Verified live: `tren benchmark baseline` now ends at the local-save success panel with no prompt, placeholder test still green, CLI-docs validator exits 0 (issue #173) | Maanas Tyagi | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-03 | Fixed every `tren tui` action button (Start/Test/Complete/Jupyter/Reset) crashing on Windows with `'charmap' codec can't decode byte 0x90`: `_run_subprocess_worker`'s `subprocess.Popen` (platforms/cli/tui/app.py) used `text=True` with no `encoding=`, so it decoded the child CLI's output with the platform default (cp1252 on Windows) instead of UTF-8 -- and the CLI's emoji banner (⚡🔥✅) is UTF-8, so the first read of any button's output raised UnicodeDecodeError and the whole action died. Same Windows cp1252-vs-UTF-8 class already fixed in jupyter.py/handler.py this session; the TUI (newer, from PR #61) was the one spot the earlier fixes never reached. Added `encoding="utf-8", errors="replace"`. An AST sweep of the whole platforms/ tree for text-mode subprocess calls missing `encoding=` confirmed this was the last remaining instance -- now zero. Verified live: running the exact Start-button command and reading its output with cp1252 reproduces the user's exact error (byte 0x90, position 236), with utf-8+replace it reads cleanly (issue #175) | Maanas Tyagi | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed three test-quality bugs that could not catch the regressions they claimed to. `test_tensor_copy_semantics` built `t2` from `original_data.copy()`, already independent of `original_data` before `Tensor()` even ran, and never checked `t1` after mutation, so it could not catch `Tensor.__init__` failing to copy defensively. Rewrote it to construct directly from `original_data` (float32, matching Tensor's internal dtype so a dtype-cast copy cannot mask an aliasing bug), mutate afterward, and assert `t1.data` is unaffected. `test_softmax_special_interface_requirements` had a comment claiming Softmax "needs 2D input," contradicting its own 1D unit test; corrected the comment to describe what the test actually verifies (row-wise softmax over batched 2D input preserves shape). `test_activation_error_handling_with_tensors` had a tautological `except (ValueError, TypeError) as e: assert isinstance(e, (ValueError, TypeError))`, true by construction. Confirmed `relu(Tensor(np.array([])))` never actually raises, so restructured the test to directly assert the real graceful-handling behavior (empty in, empty out) instead of catching-and-trivially-asserting. Verified with mutation testing on bug 1: swapping `np.array` for `np.asarray` in the Tensor constructor made the rewritten test fail with the expected aliasing symptom, reverted after confirming. Full affected suite (38 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed a critical autograd bug found during a full pre-release bug sweep (#179): `backward()` cleared a tensor's `_grad_fn` after its first invocation, so a non-leaf tensor consumed by more than one downstream op (residual connections, shared Q/K/V, weight reuse) only ever propagated its FIRST branch's gradient contribution upstream, silently dropping the rest with no error. Repro: `x=Tensor([2.0]); y=x*3; a=y*2; b=y*5; c=a+b; c.backward()` gave `x.grad=6` instead of the correct `21`. Rewrote `backward()` to use the standard topological-sort approach real autograd engines use: discover the reachable subgraph and count each node's expected incoming contributions first, then only propagate a node through its `_grad_fn` once every contribution has actually arrived. Verified: the diamond-graph repro now gives `x.grad=21`; added `test_diamond_graph_sums_both_branches` and `test_three_way_shared_node` regression tests; full `06_autograd` suite (49 tests) and downstream modules relying on autograd (07, 08, 09, 11, 12, 13, 203 tests) all green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed two bugs in the layers module found during a full pre-release bug sweep (#179): `Sequential.forward` caught bare `TypeError` to detect whether a layer's `forward()` accepts a `training` kwarg, silently swallowing any unrelated `TypeError` raised from inside a layer's own logic and masking real bugs. Now inspects the layer's `forward` signature up front instead of catching-and-guessing, in both the source module and the exported package. Verified: a layer whose forward() deliberately raises TypeError now propagates it instead of being silently swallowed. Also fixed two dropout tests that seeded a local `rng` variable that never reached `_generate_dropout_mask` (which reads the module-level global), so the seeding was a no-op despite a "For reproducible test" comment; now uses `global rng` so the seed actually reaches the code under test, plus an explicit same-seed-same-mask reproducibility assertion. Full `03_layers` suite (57 tests) plus both dropout tests run directly, green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 6 documentation/dead-code bugs across milestones 01-03 found during a full pre-release bug sweep (#179): milestone 03's docstring claimed 2,378 parameters when the formula and the script's own printed output both give 2,410; milestone 02's SGD comment claimed 13 parameters when the formula and the script's own printed output both give 17; milestone 01 computed z values and discarded them despite a comment implying they'd be shown, now displayed in the results panel; milestone 02's `evaluate_and_celebrate` discarded a recomputed accuracy value, now asserted to match the training history's own recorded accuracy as a sanity check; milestone 03's train/test accuracy delta hardcoded a literal `+` and an up arrow regardless of sign, which would have rendered incorrectly on any negative delta, now sign-aware; milestone 02's `describe_decision_boundary()` was defined but never called, now wired into the crisis-demo results table as a new column. Ran all 3 affected milestone scripts end to end, no errors | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed three bugs in the trentorch package found during a full pre-release bug sweep (#179): `BatchNorm2d.forward` set `requires_grad=True` on its output but never attached `_grad_fn` (unlike Conv2d/MaxPool2d/AvgPool2d in the same file), silently breaking backprop through any BatchNorm2d layer, gradients never reached gamma, beta, or the layer's input. Implemented `BatchNorm2dBackward`, fixed in both the exported package and the source lesson module. `olympics.SimpleMLP` used `Linear`/`ReLU` without importing them, raising `NameError` the instant the capstone demo model was constructed. Added the missing imports. `Trainer.trainer_train_epoch` used `if accumulated_loss > 0` as a proxy for "unflushed batches remain," so a leftover batch with exactly-zero loss skipped the optimizer update and gradient zeroing, leaking gradients into the next epoch; now tracks an explicit batch counter. Verified: BatchNorm2d gradients now populate on x/gamma/beta after backward(); SimpleMLP constructs without error; full 09_convolutions suite (40 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 3 bugs in the optimizers/training modules found during a full pre-release bug sweep (#179): checkpointing only saved momentum buffers via `hasattr(optimizer, 'has_momentum')`, which only SGD defines, so Adam/AdamW checkpoints silently dropped their m/v moment buffers and step count, resetting bias correction to step 1 on resume with no warning. Added a uniform `get_state()`/`set_state()` interface to every optimizer class (source and exported package), so checkpointing needs no special-casing per optimizer type. Also fixed the trailing partial-accumulation window in `trainer_train_epoch`: its pre-scaled loss was averaged as if it were a full window (understating reported loss whenever dataset size isn't divisible by accumulation_steps), its optimizer update didn't increment `self.step`, and both fixes needed to be paired with the explicit-batch-counter fix from a related PR to fully close the gradient-leak path. Verified: AdamW's m/v buffers and step count now round-trip exactly through a checkpoint save/restore (new `test_adamw_checkpoint_state_round_trips`); a leftover zero-loss batch correctly triggers its optimizer update instead of leaking gradients (new `test_trailing_zero_loss_batch_does_not_leak_gradients`). Full 07_optimizers/08_training suites (76 tests) plus downstream 09_convolutions/13_transformers (68 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 3 bugs in tokenization/convolutions found during a full pre-release bug sweep (#179): `BPETokenizer.train` built word frequencies from raw corpus strings without splitting on whitespace, so a multi-word entry like "hello world" was treated as one 12-character word including the space, letting BPE learn merges that span a space character; those merges can never fire at encode time (which splits on whitespace first), wasting vocabulary capacity. Now splits into words the same way `encode()` does, fixed in both source and exported package. The dedicated "Module 10 progressive" test file never imported or exercised any tokenization code at all despite its own header claiming to test it, instead re-testing Trainer/optimizer machinery; added real CharTokenizer/BPETokenizer round-trip, unknown-character, and space-merge regression tests. `test_maxpool2d_forward`/`test_avgpool2d_forward` built inputs shaped `(1,4,4,1)` intending a 4x4 image, but this module's tensors are NCHW, so that shape was actually channels=4, width=1, producing an unexpected output shape that made the value-checking assertions sit behind an always-false `if output.shape == (1,2,2,1)` guard, dead code that could never catch a broken max/average implementation. Corrected to `(1,1,4,4)` and made the assertions unconditional. Verified: mutation-tested the BPE fix (reverting it reproduces a `('hello', ' ')` merge, caught by the new test); full 09_convolutions/10_tokenization suites (64 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 5 bugs in embeddings/attention modules found during a full pre-release bug sweep (#179): sinusoidal positional encoding had no `seq_len > max_seq_len` bounds check (unlike the learned-encoding branch), so a too-long sequence silently sliced a shorter table and failed later with a cryptic numpy broadcast error instead of a clear one; added the same kind of explicit check. `MultiHeadAttention`'s invalid-config error message suggested `num_heads=embed_dim // (embed_dim // num_heads)`, which is mathematically guaranteed to re-suggest the exact same invalid value the caller just passed; now searches for the largest actual divisor, fixed in both source and exported package. `test_positional_embedding` imported a class name (`PositionalEmbedding`) that doesn't exist, vacuously passing via a caught `ImportError`; corrected to the real `PositionalEncoding` and rewrote it to verify real behavior (shape, that positions are actually added, and the new bounds-check error). `test_embedding_sparse_gradients` only checked the gradient tensor was non-zero overall, which would pass even with a broken scatter; now asserts unaccessed vocabulary rows are exactly zero and accessed rows are non-zero. A memory-overhead analysis printed per-stage increments under headers that implied running cumulative totals; now prints true cumulative sums matching the headers. Full 11_embeddings/12_attention suites (60 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 3 bugs in transformers/profiling modules found during a full pre-release bug sweep (#179): `measure_memory`'s returned `peak_memory_mb` was clamped to `max(peak_memory_mb, useful_memory)`, but `memory_efficiency` was computed from the unclamped raw value, so the two numbers in one returned dict were computed from different bases; now both use the same clamped value, fixed in both source and exported package. Two transformer tests built masks as additive `-inf`/`-1e9` values, but this module's documented masking contract requires a binary 1=allow/0=block mask (`_apply_mask` computes `adder = (1-mask)*MASK_VALUE`), so those tests' masks were structurally incompatible with the real masking implementation, and both tests only checked output shape, never masking correctness; switched both to the real `create_causal_mask()` and added assertions that future positions actually receive ~0 attention weight. Two profiling tests imported classes (`MemoryProfiler`, `profile_memory`, `Timer`) that don't exist anywhere in the module, vacuously passing via a caught `ImportError` regardless of whether profiling worked; rewrote both against the real `Profiler` API (`measure_memory()`, and use as a context manager for timing). Full 13_transformers/14_profiling suites (50 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 5 bugs in quantization/compression modules found during a full pre-release bug sweep (#179), 2 of them real numerical correctness bugs. `quantize_int8`'s standard branch computed `zero_point` then clamped it to `[-128,127]` without adjusting `scale` to compensate, silently breaking the affine mapping whenever a tensor's range didn't straddle zero: measured max round-trip error dropped from ~5x the module's own documented tolerance to well within it after the fix. The constant-tensor special case hardcoded `scale=1.0`, so `zero_point=round(-c)` could only encode the nearest integer to `c`, destroying precision for non-integer constants (a constant of 0.1 dequantized to 0.0); a constant tensor now round-trips exactly regardless of magnitude. Both fixed via a shared `_affine_qparams` helper (extend the range to include 0 before computing scale, the standard fix), applied at every call site in both source and exported package. `compress_model` silently accepted a `'low_rank'` config key and reported it as applied without touching the model at all; now raises `NotImplementedError` with actionable guidance instead of lying about what happened. Implemented both empty integration test stubs (quantization round-trip/matmul/memory-savings, compression pruning/sparsity) that previously always passed via a bare `pass`. Full 15_quantization/16_compression suites (19 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 6 bugs in acceleration/memoization modules found during a full pre-release bug sweep (#179), including a real correctness bug in generated model output. `analyze_kvcache_memory` multiplied by `_BYTES_PER_FLOAT32` twice when computing model memory, inflating it 4x, which made the printed "Memory Overhead" percentage 4x too small (and coincidentally still fell inside the docstring's claimed range, reinforcing the wrong formula). The prefill loop's first-token path never wrote that token's real K,V into the KV cache (only `advance()` ran, not `update()`), permanently leaving position 0 as zeros, so every subsequent generated token silently attended to a fake first token instead of the real one, contradicting the module's core claim that caching doesn't change results; now writes real K,V for the first token too. `test_kv_cache_exists`/`test_kv_cache_initialization`/`test_kv_cache_update` only checked hasattr() or vacuously passed via caught ImportError, never that update()+get() actually round-trip real data; rewrote to assert real behavior unconditionally. `test_memoization_decorator` imported a `memoize` function that doesn't exist anywhere in the module; removed (confirmed no such API is otherwise implemented). `test_acceleration_integration` and the old `test_tinygpt_integration` (which imported a module path, `trentorch.tinygpt`, that has never existed in this package, and used a non-`test_`-prefixed function name so pytest never even collected it) were both empty or dead; rewrote both against the real public API end to end. A "Phase 2/Phase 4" printout was missing its "Phase 3" header, leaving three tiling-related bullets orphaned; added the missing header. Full 17_acceleration/18_memoization suites (38 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 5 bugs in benchmarking/capstone modules found during a full pre-release bug sweep (#179). `MLPerf.run_standard_benchmark` regenerated its RNG seed INSIDE the per-run loop, so every one of `num_runs` "runs" got bit-identical input tensors, meaning the reported p50/p90/p99 latency spread reflected only system timing noise, not input diversity, undermining the module's own "statistical rigor" teaching goal; now seeds once before the loop. `generate_submission`'s `compression_ratio` had no zero-floor on its denominator, unlike the identical-purpose `speedup` calculation two lines above, so a degenerate optimized model reporting 0 parameters raised `ZeroDivisionError`; floored the same way, with a new regression test. `test_unit_json_serialization` used a hardcoded `/tmp/...` path, which resolves relative to the current drive's root on Windows and fails with `FileNotFoundError`; switched to `tempfile.TemporaryDirectory()` matching the rest of the module. `_collect_base_metrics`/`analyze_optimization_techniques` matched model names via plain substring containment, so overlapping names (e.g. "model" vs "model_v2") could silently cross-match depending on dict iteration order, corrupting every downstream speedup/compression comparison; switched to exact matching. Implemented both empty integration test stubs (benchmarking: real latency benchmark on two models; capstone: full baseline-vs-optimized submission generation) that previously always passed via a bare `pass`. Full 19_benchmarking/20_capstone suites (18 tests, excluding one pre-existing environment-only console-encoding failure unrelated to this fix) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed 7 bugs across tests/dev_tools/web found during a full pre-release bug sweep (#179). `tests/integration/test_training_capabilities.py`: every `test_*` function returned a bool instead of asserting, so pytest marked them "passed" regardless of the returned value; converted all 7 to real assertions, which surfaced a genuinely hidden bug in `test_gradient_flow` (never created an optimizer, so `requires_grad` was never enabled on the model's weights, and gradients could never populate regardless of correctness), now fixed by constructing the optimizer the test always should have. `tests/regression/test_transformer_reshaping.py`: `test_attention_kv_cache_shapes` had no return statement, so `all_pass &= test_attention_kv_cache_shapes()` raised `TypeError` and crashed the whole file before later tests ran; added the missing `return True`. `test_loss_backward_integration` and `test_training_loop_integration` only asserted trivial preconditions (loss is not None) without ever calling backward()/step() or checking their effects; both now exercise the real integration and assert gradients/weight changes actually happened. `test_chain_rule_linear_relu` computed `z.data > 0` and discarded it, never actually checking the claimed "ReLU only backprops where input > 0" behavior; now asserts it directly. `run_sandbox_tests.py`'s `"__main__" in dir(module)` branch was dead code (checks attribute names, not execution state) and always fell through to the working else branch anyway; removed. `platforms/web/js/app.js`'s unused `digitGrid` state field, which implied pixel capture for a prediction pipeline that doesn't exist, removed. Full affected suites (41 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Reviewed maanas1234's PR #176 (fixed tui/app.py's Popen text=True missing encoding=, crashing every TUI action button on Windows with UnicodeDecodeError decoding the CLI's UTF-8 emoji banner as cp1252) and swept the rest of the repo for the same gap. Found and fixed three more instances the PR's own platforms/-scoped AST sweep didn't cover: data/milestones/tests/test_milestones_run.py (runs milestone scripts, whose own output includes an emoji banner, via subprocess.run with text=True and no encoding=, hitting the identical crash on Windows), platforms/cli/tests/test_jupyter_server_startup.py (same gap on a jupyter --version check), and .github/scripts/update_contributors.py's two gh CLI subprocess calls (would crash on a non-ASCII contributor display name under a non-UTF-8 locale). Added encoding="utf-8", errors="replace" to all four, matching the pattern already established everywhere else in platforms/ | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Feature: bare `tren` (no command, no flags) now launches the interactive TUI dashboard directly once a student is past their first-ever run, instead of only printing a welcome panel with a "run tren tui" hint every time. Reused the exact routing already used by the existing `tren --tui`/`-i` shortcut. First run is unchanged: the one-time welcome/quick-start panel still shows before a student is ever dropped into a full-screen TUI with no explanation. Fixed 7 existing tests that asserted on the bare-tren welcome screen via subprocess and would otherwise hang waiting on the TUI's interactive event loop; added a shared force_first_run fixture (platforms/cli/tests/conftest.py) plus a 30s subprocess timeout safety net, and two new unit tests exercising the routing decision directly via monkeypatching. Full platforms/cli/tests suite (500 tests) and tests/e2e/test_user_journey.py quick suite (17 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed `tren` resolving the wrong project root when run from any directory outside a TrenTorch checkout (e.g. right after `cd ~`): `CLIConfig.from_project_root()`'s cwd-based walk-up silently fell back to treating cwd itself as the project root when it found no pyproject.toml, so `user_data/`, `data/src/`, etc. got looked up in the wrong place, even though `tren` itself (a console-script entry point) is found via PATH regardless of cwd. Reused `modules.py`'s existing `_find_project_root()` (walks up from that file's own installed location, already correct) as the fallback instead of cwd, keeping the cwd-walk as the first attempt so a maintainer juggling multiple checkouts still resolves to whichever one they cd into. Mutation-tested: reverting the fix reproduces the exact original symptom (project_root == the unrelated directory). Full platforms/cli/tests suite (503 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Added an opt-in Step 5/5 to `tren setup`: if a venv exists and its bin directory isn't already on PATH, asks whether to add it (default yes, skippable via a new `--skip-path` flag or by declining). Closes the remaining gap in "type `tren` from any terminal, any directory" (#198, #199): `tren` is a console-script entry point found via PATH regardless of cwd, but nothing previously persisted the venv's bin directory onto PATH, so only a terminal where the venv happened to already be activated could find it at all. Persists via HKEY_CURRENT_USER\Environment\Path (winreg) on Windows, or an idempotent marked `export PATH=...` line appended to the shell rc file detected from $SHELL on macOS/Linux; new terminals opened after setup pick it up, the terminal running setup itself is unaffected until reopened. New `platforms/cli/core/path_manager.py` keeps the real-environment-mutating functions separate from pure decision logic, fully unit-tested without touching the real registry or a real rc file (22 new tests). Full platforms/cli/tests suite (525 tests) green | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Added a Quick Start section to README.md, immediately after the hero/badges block, before any pitch content -- previously the README had no clone-and-run instructions at all, just narrative sections. Uses the same venv-then-pip sequence already documented in docs/CONTRIBUTING.md, verified against a genuinely fresh Python interpreter with no packages pre-installed: the initially-tempting `python bin/tren setup` shortcut (bin/tren's own docstring claims "no pip install required") actually fails immediately with `ModuleNotFoundError: No module named 'rich'`, since main.py imports rich-dependent modules at top level before any command dispatch -- that docstring only means no `pip install -e .` for the trentorch package itself is required, not that no dependencies are needed at all. Ends with plain `tren` (#198's bare-tren-launches-TUI) and a callout about #200's PATH-persistence prompt. Verified via validate_cli_docs.py (introspects the live CLI's argparse tree, confirms `tren setup` is a real command path) and by manually running the documented sequence end to end in a fresh venv | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Fixed misleading Windows activation instructions in the README Quick Start section (#201): the line `source .venv/bin/activate # On Windows: .venv\Scripts\activate` used a bash-only `source` command and a Windows comment missing the `.ps1` extension PowerShell actually needs, so a user following it literally in PowerShell hit `source: The term source is not recognized...`. Split into separate bash and PowerShell code blocks, matching docs/CONTRIBUTING.md, and added the common execution-policy gotcha PowerShell users hit on the activation script. Verified: validate_cli_docs.py still passes | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Bumped version to 1.0.0 for the v1 release, in both `pyproject.toml` and the duplicate `settings.ini` (nbdev's own metadata file, a known drift risk per docs/implementation.md since nothing mechanically syncs the two). Updated docs/implementation.md's two mentions of the old `0.0.1` to match | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Added a `class="contributor-avatar"` attribute to the Team Engineers table's avatar `<img>` tags (in `.github/scripts/update_contributors.py`, the nightly generator, and the current README.md), so the trentorch-website's markdown renderer can style them as uniform circular avatars -- the website's Tailwind Typography prose styles (prose-img:rounded-none, plus default prose img sizing) were otherwise stretching each avatar to its table cell's full width with no rounding, so avatars with different source-image aspect ratios rendered visibly different sizes. Harmless on GitHub's own README rendering, which ignores unrecognized class attributes. Verified: update_contributors.py --selftest and validate_cli_docs.py both pass | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-04 | Removed em dashes and double hyphens from README.md and the Team Engineers table generator (.github/scripts/update_contributors.py), replacing them with colons/commas per house style. Two of the three were introduced by me earlier this same session (Quick Start PATH callout, the generated Team Engineers intro sentence). Also cleaned the trentorch-website repo's own source (page copy, code comments) in a companion commit there. Verified: update_contributors.py --selftest and validate_cli_docs.py both pass | Rocky | Shashank-Tripathi-07 | Shashank-Tripathi-07 |
| 2026-09-05 | Fixed `tren module start`/`resume` getting permanently stuck when `data/modules/` and `user_data/progress.json`'s `started_modules` drift apart (e.g. after `tren system reset --keep-progress --force`): `start` refused forever with "already started", and `resume` accepted then failed deep inside `open_jupyter()` with a generic, unhelpful error. Both now check whether the notebook actually exists on disk before trusting the flag, and self-heal by recreating it from `data/src/` via the existing export helper; `open_jupyter()`'s dead-end message now also suggests `tren module reset N --force` (issue #142). Also closed three CI gaps found while landing this: added the same `--no-jupyter` CI/testing escape hatch `start` already had to `resume` (its regression test was spawning a real detached Jupyter Lab server with no cleanup and hanging past its own timeout), hash-pinned `colorama` and a handful of other Windows/macOS-only transitive deps that `--require-hashes` was silently missing in `requirements.txt`, and fixed a `ruff format` violation in the original diff. Verified: full regression test plus the affected `platforms/cli/tests/` suite (141 tests) green locally on Windows, no orphaned processes left behind, `ruff check`/`ruff format --check` both clean, `pip install --require-hashes -r requirements.txt` dry-run succeeds | Mahek Patel | Rocky | Rocky |
