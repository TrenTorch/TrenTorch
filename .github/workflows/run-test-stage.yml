name: 'Reusable · Run Test Stage'

# Backs Stages 1-5 and 7 of validate.yml. A reusable workflow (job-level
# reuse via workflow_call) rather than a composite action (step-level
# reuse): a composite action still needs the calling job to check out the
# repo itself before `uses: ./local-path` can even be resolved, which
# leaves one checkout step duplicated in every caller. A reusable workflow
# owns the whole job, checkout included, so the caller in validate.yml is
# reduced to a single `uses:` + `with:` block per stage.
#
# Called workflows don't inherit the caller's top-level `env:` context, so
# the shared env vars are redeclared here rather than in validate.yml.

on:
  workflow_call:
    inputs:
      os_matrix:
        description: 'JSON array of runner OS values, e.g. ["ubuntu-latest","windows-2022"]'
        required: true
        type: string
      test_flag:
        description: 'Flag passed to `tren dev test`, e.g. --unit, --integration'
        required: true
        type: string
      step_label:
        description: 'Human-readable label for the job name, e.g. "Stage 2 · Unit Tests"'
        required: true
        type: string
      download_package:
        description: 'Whether to download the Stage 1 built-package artifact first'
        required: false
        type: string
        default: 'true'
      upload_package:
        description: 'Whether to upload data/trentorch/ and data/modules/ as the built-package artifact after'
        required: false
        type: string
        default: 'false'
      timeout_minutes:
        description: 'Per-job timeout'
        required: false
        type: number
        default: 30
      advisory_windows:
        description: 'Whether a Windows-leg failure is advisory (continue-on-error) rather than blocking'
        required: false
        type: string
        default: 'true'

# Empty at workflow level (matches codeql.yml / semantic-pr-title.yml's
# pattern for the same OpenSSF Scorecard finding); the job below declares
# the one permission it actually needs.
permissions: {}

env:
  TREN_ALLOW_SYSTEM: "1"
  TRENTORCH_QUIET: "1"
  PYTHONIOENCODING: "utf-8"  # Required for Windows emoji support in Rich
  PYTHONUTF8: "1"            # Force UTF-8 for all Python I/O (subprocess pipes)

jobs:
  run:
    name: ${{ inputs.step_label }} (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: ${{ inputs.timeout_minutes }}
    continue-on-error: ${{ inputs.advisory_windows == 'true' && contains(matrix.os, 'windows') }}
    # Only checkout + artifact upload/download, neither of which needs
    # anything past read access to repo contents (artifact steps use their
    # own signed-URL storage API, not GITHUB_TOKEN's repo permissions).
    # No explicit permissions here previously, so this job silently
    # inherited whatever the repo/org default was rather than declaring
    # what it actually needs -- OpenSSF Scorecard's Token-Permissions
    # check flags that as unauditable even when the inherited default
    # happens to already be safe.
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJson(inputs.os_matrix) }}

    defaults:
      run:
        shell: bash
        # All CLI code/config lives under TrenTorch_CLI/ now (.github/
        # itself didn't move -- GitHub only discovers workflows at the true
        # repo root). Only affects `run:` steps; `uses:`-based steps below
        # (cache, artifact up/download) need their own explicit prefix.
        working-directory: TrenTorch_CLI

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: TrenTorch_CLI/requirements.txt

      # setup-python's cache: 'pip' input only caches pip's download cache
      # (compressed wheels), not the installed packages themselves, so
      # every job still pays the extract-and-install cost from scratch.
      # This caches both directories pip actually writes to: site-packages
      # (the importable package code) AND the scripts directory (the
      # console-script executables pip generates, e.g. jupyter, jupyter-lab).
      # A prior version of this cached only site-packages: pip saw the
      # packages already present on a cache hit and skipped regenerating
      # the console scripts, so `jupyter` ended up completely missing even
      # though `import jupyter` worked fine. Caching both together, as one
      # cache entry, keeps them consistent with each other.
      - name: Resolve site-packages and scripts paths
        id: python-paths
        run: |
          python -c "import sysconfig; print('purelib=' + sysconfig.get_paths()['purelib'])" >> "$GITHUB_OUTPUT"
          python -c "import sysconfig; print('scripts=' + sysconfig.get_paths()['scripts'])" >> "$GITHUB_OUTPUT"

      - name: Cache installed dependencies
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ${{ steps.python-paths.outputs.purelib }}
            ${{ steps.python-paths.outputs.scripts }}
          key: py-deps-${{ runner.os }}-py3.11-${{ hashFiles('TrenTorch_CLI/requirements.txt') }}

      - name: Install dependencies
        # requirements.txt is a pip-compile-generated, hash-pinned lockfile
        # (see its header, and requirements.in for the source policy);
        # --require-hashes is what satisfies OpenSSF Scorecard's
        # Pinned-Dependencies check. pip itself is pinned the same way ruff
        # is pinned in lint.yml/autofix.yml: exact version + real PyPI
        # sha256, one line, not a heredoc (a heredoc's closing marker has to
        # sit at column 0, which doesn't survive an indented YAML block
        # scalar).
        run: |
          printf '%s\n' 'pip==26.2.1 --hash=sha256:71138adf1f4ca900cdb7d289c21b7494329f2332b6d85f0e1c42108c0384ed3e --hash=sha256:f6ad667e89a1fe78046c8f13232b247200f5258d7828f3f7883d660878e0813f' > /tmp/pip-pin.txt
          python -m pip install --require-hashes -r /tmp/pip-pin.txt
          pip install --require-hashes -r requirements.txt

      # actions/upload-artifact strips the shared parent directory (data/)
      # from archived paths when given multiple search paths under it (its
      # own "least common ancestor" behavior) -- the zip holds
      # trentorch/... and modules/..., not data/trentorch/... and
      # data/modules/.... Extracting explicitly into data/ here undoes
      # that stripping so the files land back where they actually live.
      #
      # Unsuffixed name (not built-package-${{ matrix.os }}): the built
      # package (data/trentorch/, data/modules/) is pure Python source with
      # nothing OS-specific in it, so validate.yml's Stage 1 now produces
      # exactly one canonical artifact from its Ubuntu leg -- the one every
      # downstream OS leg downloads, Windows included. Stage 1's own
      # Windows leg still builds and tests everything for coverage, it just
      # doesn't upload an artifact anyone consumes (see upload step below).
      - name: Download built package from Stage 1
        if: inputs.download_package == 'true'
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: built-package
          path: TrenTorch_CLI/data

      - name: Run tests
        run: ./bin/tren dev test ${{ inputs.test_flag }} --ci

      # Only Stage 1's Ubuntu leg sets upload_package: 'true' (see
      # validate.yml): Stages 2-5 each need the exported package present to
      # skip their own redundant `tren dev export --all` (see
      # platforms/cli/cli_platform/dev/test.py's _check_imports/_build_package). Sharing
      # that one build's output cuts the duplicated work to zero without
      # skipping or altering any test. Unsuffixed name -- see the matching
      # comment on the download step above.
      - name: Upload built package
        if: inputs.upload_package == 'true' && success()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: built-package
          path: |
            TrenTorch_CLI/data/trentorch/
            TrenTorch_CLI/data/modules/
          retention-days: 1
